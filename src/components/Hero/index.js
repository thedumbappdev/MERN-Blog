import React from 'react';
import Logo from '../Logo';
import Navbar from '../Navbar';
import Card from '../UI/Card';

const Hero = () => {
    return (
        <Card>
            <Logo />
            <Navbar />
        </Card>
        
    );
}

export default Hero;
